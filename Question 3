get(key)
put(key, value)
remove(key) 

#Recursive search in a BST
function TreeSearch(position, key)
	if positional is external then
		return position
	else if key == key(position) then
		return position
	elseif key < key(position) then
		return TreeSearch(left(position), key)
	else
		return TreeSearch(right(position), key)

#Inserting a key-value pair into a map that is represented as a BST
function TreeInsert(key, value)
	position = TreeSearch(root(), key)
	if key == key(position) then
		position = value
	else
		expandExternal(position, (key, value))

